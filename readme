直接运行vrp.py程序即可，在运行前，请确认可以import tqdm包，tqdm的作用是以进度条形式显示任务进度和预计完成时间，使用pip install命令即可下载


文件说明：
data_utils.py 数据初始化类
vrp_utils.py  文件进行节约法的相关操作

route_utils.py 路径类，对路径进行优化及判断是否符合约束条件
vrp.py 主程对data目录下的数据使用节约法进行优化路径





算法说明：

1、初始化路径， 每个节点为一条路径， 即[0;x;0]或者[0;x;充电站;0]， 并计算每条路径的总成本

2、遍历路径， 计算两两路径的节约成本 
3、按节约成本从大到小进行排序，取前N条新路径替换被合并的路径，重新执行上述操作  
举例：
    
A 路径：0;a;0  成本cost_a
    
B 路径：0;b;充电站;0  成本cost_b
    
提取A、B路径中需要服务的节点即a, b
        
对a, b进行服务时间的先后顺序进行排序
        
在开始处插入中心节点
        
分别对两种车型进行：
           
      计算里程，在适当的地方插入充电站， 及路径末尾插入中心节点
            
      对从中心节点出发的时间进行优化
            
      计算总成本cost_c
        
取两种车型cost_c的最小值
，节约的成本为 cost_a + cost_b - cost_c
